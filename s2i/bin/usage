#!/bin/bash -e
cat <<EOF
This is the drupal8-site-install-builder S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> drupal8-site-install-builder <application image>

You can then run the resulting image via:
docker run <application image>
EOF
